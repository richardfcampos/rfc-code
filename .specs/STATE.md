# RFC Code — Project State

## Decisions Log

| ID | Date | Decision | Source |
| --- | --- | --- | --- |
| AD-001 | 2026-07-23 | Deploy em produção no servidor local **intel** | Usuário |
| AD-002 | 2026-07-23 | Acesso via **Tailscale sem login no app**; perímetro = tailnet; sem exposição pública | Usuário |
| AD-003 | 2026-07-23 | Agents do MVP: **Claude Code + Codex + OpenCode + Cursor** (Cursor com risco sinalizado) | Usuário |
| AD-004 | 2026-07-23 | **Tablet obrigatório** no MVP; phone P2; desktop-first | Usuário |
| AD-005 | 2026-07-23 | Base: **fork de siteboon/claudecodeui** (AGPLv3 ok p/ self-host pessoal) — CONFIRMADO pelo usuário | Usuário + pesquisa (3 researchers) |
| AD-007 | 2026-07-23 | Nome do projeto: **RFC Code** (dir `rfc-code`) | Usuário |
| AD-008 | 2026-07-23 | Compliance AGPL-3.0 + Seção 7 do upstream: manter atribuição "CloudCLI UI (github.com/siteboon/claudecodeui)" no README do fork; marcar como versão modificada; nunca usar marcas CloudCLI/Siteboon | Verificação (`LICENSE:668-695`, NOTICE) |
| AD-009 | 2026-07-23 | Estratégia de fork: branch `main` espelha upstream, trabalho em branch `rfc-code`; código novo em módulos novos; NÃO podar código upstream (minimizar conflitos de merge) | Design |
| AD-010 | 2026-07-23 | Linguagem: **híbrido** — backend do fork permanece Node/TS; componentes CPU-bound futuros nascem como sidecar Rust/Go (nenhum no MVP) | Usuário |
| AD-011 | 2026-07-23 | Perfis: **abordagem A** (env-injection por sessão, container único) — única compatível com troca de conta barata. + HUB-12 handoff entre turnos no MVP (P2). **Colaboração multi-conta** (planner/executor, review cruzado, debate) = feature 2 `multi-account-collab`, spec próprio | Usuário (escopo) + Design (sequenciamento) |
| AD-012 | 2026-07-23 | Execução: orquestrador (Fable) NÃO implementa — 1 worker por fase com roteamento de modelo: **Sonnet** fases mecânicas (1, 3, 4), **Opus** fases críticas (2 perfis, 5 handoff) e **Verifier** | Usuário |
| AD-013 | 2026-07-23 | Resiliência a limite de uso: scheduled task `rfc-code-resume-execute` (a cada 2h) lê Handoff e retoma Execute se heartbeat "Última atividade" > 90min e trabalho incompleto. TODA sessão executando DEVE atualizar o heartbeat no Handoff após cada task/fase | Usuário |
| AD-006 | 2026-07-23 | Isolamento multi-conta: 1 perfil = 1 config dir isolado em volume (`CLAUDE_CONFIG_DIR` p/ Claude; auth.json isolado p/ Codex/OpenCode/Cursor) | Pesquisa |
| AD-014 | 2026-07-23 | ~~Produção = intel É o macOS local. Acesso canônico: **http://intel:7789** (publish só na interface tailscale `100.70.101.109` via bridge; `network_mode: host` não funciona no Docker Desktop macOS). `tailscale serve` HTTPS opcional p/ PWA. Dados persistentes em `~/.rfc-code/data`; projetos em `/Volumes/External Code/INTEL/Code`~~ **SUPERSEDIDA por AD-015** | Usuário (acesso/porta) + verificação empírica (rede) |
| AD-015 | 2026-07-30 | **Instalação NATIVA substitui Docker** (feature `native-install`). Motivo do usuário: container restringe filesystem. macOS: LaunchAgent no login + KeepAlive (LaunchDaemon rejeitado: TCC/keychain/volume externo); Linux: systemd user unit + linger. Bind tailnet direto via contrato `AUTH_TRUSTED_NATIVE_BIND=1` (wildcard recusado). Dados `~/.rfc-code/data` (migrados do container c/ rewrite de paths); config `~/.rfc-code/env`. `deploy/` REMOVIDO do repo (decisão do usuário, contra recomendação de manter p/ intel — intel migra pro instalador nativo). Armadilha conhecida: launchd+volume externo exige TCC (Full Disk Access) no binário do node; grant gruda no path (quebra em troca de versão via nvm) | Usuário (4 decisões em context.md) + verificação empírica (sample do open() travado) |

| AD-016 | 2026-07-31 | **Port cirúrgico** das features `worktrees` e `composer-model-menu` do upstream v1.37.0 (`06e7ee9` "#1037"), em vez de merge completo. Motivo: a v1.37 reescreveu `server/index.js`→`index.ts` e `server/routes/*.js`→`server/modules/*.module.ts`; merge colidiria de frente com os 64 commits do fork (native-install, profiles, providers, browser-use, rebrand). Débito: avaliar o merge completo da v1.37 como trabalho próprio depois | Usuário (opção A) + análise de divergência |
| AD-017 | 2026-07-31 | Seletor de modelo = **menu combinado modelo + effort** (`ComposerModelMenu` do upstream), substituindo o chip "Effort Default" atual — em vez de um chip separado só de modelo. Motivo: fidelidade ao upstream e menos poluição da barra no tablet (AD-004) | Usuário |
| AD-018 | 2026-07-31 | Worktrees ficam no **padrão upstream**: `<pai-do-repo>/<repo>-worktrees/<branch-sanitizada>`, sem opção configurável em Settings. Ex.: `/Volumes/External Code/M1/Code/personal/rfc-code-worktrees/feature-x` | Usuário |

| AD-019 | 2026-08-09 | `multi-account-collab`: os 3 modos do AD-011 num motor só (debate/review/vote); participantes rodam **read-only** (`permissionMode: 'plan'` + `disallowedTools` explícito) — consenso é decidir, não executar; v1 só perfis claude (único provider com plan mode e prompt de permissão alcançável); progresso por polling REST, não por evento novo no protocolo de chat | Orquestrador (usuário delegou: "use agentes como achar necessário") |

| AD-020 | 2026-08-13 | `cross-provider-session-switch`: trocar provider/conta no meio da sessão leva o contexto (primer montado do histórico normalizado, orçamento 24k chars, cauda preservada) numa **sessão nova** do provider destino — trocar `sessions.provider` no lugar é inviável porque o histórico é lido do adapter do provider (`sessions.service.ts:212-218`). Dois pontos de entrada (menu do composer + modal do header). Os 4 providers, sem hardcode de par claude↔codex | Usuário (3 respostas em `context.md`) + verificação de código |
| AD-021 | 2026-08-22 | Feature `agent-orchestration` (pacote de ausência, análise do orkestrai): Fase 1 = tasks nativas + orgs com política de perfis + quota recommend + bridge MCP. Filtro de adoção: funciona na ausência do usuário; features de presença rejeitadas. Canvas só como Team View read-only (Fase 3). Política: perfil de empresa nunca cruza org; pessoal como fallback só com primário ≥ limiar (default 85%), auditado, default deny (compat: org sem policy libera tudo) | Usuário (conversa 2026-08-22) + spec em `.specs/features/agent-orchestration/` |
| AD-022 | 2026-08-22 | Execução `agent-orchestration`: Fable orquestra somente; workers sonnet (mecânico) / opus (crítico), padrão AD-012. Branch dedicada `feat/agent-orchestration` a partir de `rfc-code` @ ce5d328 | Usuário (/tlc-spec-driven) |

| AD-021 | 2026-08-14 | `unified-session-across-providers`: trocar provider deixa de forkar a sessão. Uma sessão do app = N **pernas** (`session_legs`), uma por sessão nativa de provider; `sessions` guarda as colunas da perna ativa como projeção, então nenhum leitor atual muda. Histórico = merge das pernas por `timestamp` (não concatenação — com reativação A→B→A a concatenação dá ordem errada). Decisões do usuário: (1) voltar a um provider **retoma a perna antiga** com resume nativo e primer incremental; (2) fronteira entre pernas é **separador visível** no chat; (3) confirmação cross-provider **fica**, com texto corrigido (não promete mais sessão nova); (4) pares já forkados **são migrados** em pernas, preservando a `session_id` da origem. Rejeitado: forjar transcript nativo do destino (formato interno, `reasoning.encrypted_content` não atravessa conta) e store canônico de mensagens no banco (abandonaria resume nativo). **Revisão do mesmo dia** (usuário: "quero que mantenha a sessão como se eu não tivesse trocado"): o primer deixa de ser cauda de 24k chars e passa a levar a **conversa inteira**, com orçamento derivado da janela do modelo destino; no estouro, o trecho antigo é **resumido** (não truncado cego) por uma execução destacada via `EphemeralQuery` do `/btw` — que é **claude only**, então sem perfil claude autenticado a degradação para truncamento é o caminho normal, não a exceção. Separador visível e modal de confirmação **ficam** (reconfirmados) | Usuário (4 respostas em `context.md` + revisão) + pesquisa (`research.md`) + verificação de código |

## Blockers & Débitos

| ID | Data | Item | Estado |
| --- | --- | --- | --- |
| BK-001 | 2026-07-31 | Suíte falhava 7/256 nesta máquina: `~/.rfc-code/env:26` define `AUTH_TRUSTED_NATIVE_BIND='1'` (AD-015) e `withIsolatedAuthEnvironment` (`server/middleware/auth.test.js`) salvava/restaurava a var mas não a limpava no setup — testes que assumem ambiente sem contrato de bind falhavam. Máquina limpa/CI passaria. Corrigido em T0 da feature `worktrees` | Resolvido |
| DB-001 | 2026-07-31 | `npm test` só varre `server/**`; os `.test.ts` de `src/` (ex.: `src/components/git-panel/utils/commitGraph.test.ts`) não têm runner configurado — componentes React ficam sem cobertura executável | Aberto |
| DB-002 | 2026-07-31 | Avaliar merge completo do upstream v1.37.0 (bugfixes e refactor de módulos do servidor que o port cirúrgico do AD-016 deixou de fora). Itens conhecidos que ficaram de fora: `git init` pela UI (`notGitRepository`/`initRepository`), `ComposerPermissionMenu`, e o `sessionModel` com endpoint GET de modelo ativo da sessão | Aberto |
| DB-004 | 2026-08-13 | Handoff HUB-12 semeia contexto que nunca chega ao modelo: `handoff.service.ts:143-165` grava `handoff-seeds/<id>.jsonl` e reaponta `jsonl_path`, mas não existe consumidor em `server/**` (grep). O status `seeded` promete contexto e entrega sessão vazia | Resolvido — T3/T4/T5 de `cross-provider-session-switch` (primer markdown + `seed_primer_path` + consumo no despacho) |
| DB-005 | 2026-08-13 | Fila de troca do handoff é código morto: `markSessionRunning`/`markSessionIdle`/`drainPendingSwitch` (`handoff.service.ts:65-76,226`) não têm caller em `server/**`. `defaultIsSessionRunning` sempre devolve `false` → status `queued` inalcançável e troca enfileirada nunca dreanaria | Aberto — endereçado por T6/T7 de `cross-provider-session-switch` |
| DB-003 | 2026-07-31 | Painel Git (nosso e o do upstream) não tem i18n — todas as strings hardcoded em inglês, inclusive a aba Worktrees nova | Aberto |

## Handoff

- **Última atividade:** 2026-08-14 — feature `unified-session-across-providers` **EXECUTADA por completo (T1–T21 + T6b)**. Orquestração: Fable só orquestrador, 19 workers roteados por modelo (Opus nas críticas: T5 handoff-leg, T7 merge de histórico, T10 roteamento, T16 migração de pares, T20/T21 orçamento+resumo do primer; Sonnet no mecânico; Haiku no T6 trivial). Sessão do app agora tem "pernas" (`session_legs`): trocar provider/conta abre ou reativa uma perna sem nunca mudar `session_id`; histórico é merge cronológico entre pernas com separador de fronteira; contexto atravessa inteiro, resumido só no estouro do orçamento (derivado da janela do destino). Gate final: **592/592 testes, typecheck 0, eslint 0 erros (245 warnings pré-existentes), build EXIT 0**.
  **Incidentes durante a execução, registrados porque mudam como orquestrar daqui pra frente:**
  1. Host (VPS) sem swap — 6 OOM-kills do `rfc-code.service` no mesmo dia, correlacionados com a carga paralela desta orquestração empilhada em cima de processos alheios ao projeto (2 dev-servers Next.js + 10+ sessões `ccd-cli` remotas acumuladas). Corrigido com swapfile de 8GB (`/swapfile`, persistido em `/etc/fstab`). Detalhe em memória `vps-oom-no-swap`.
  2. Cada crash derrubou o processo do Claude Code em execução (não o app em si na maioria dos casos — só uma vez foi o `rfc-code.service`) e **perdeu agentes em background sem aviso confiável**: a notificação de conclusão de um agente às vezes nunca chegou, e a contagem agregada de testes subindo não provava que a task específica tinha rodado — T21 foi marcada completa por engano nesse padrão (só T4 tinha de fato mudado o arquivo) e precisou ser redisparada do zero. Lição aplicada no resto da execução: **verificar o diff real de cada arquivo contra a task antes de marcar completo, nunca confiar só na contagem agregada do gate**.
  3. Dois agentes (T11, T15) encontraram premissa errada no meu próprio brief (T11: `'seeded'`/`seededSessionId` ainda são produzidos por um caminho same-provider não tocado por esta feature; a instrução original mandava apagar o campo) e corretamente devolveram BLOCKED em vez de adivinhar — prova de que o formato de report exigido (Status/Summary/Files/Gate/Concerns) funciona.
  **Pendente: T17 (smoke com contas reais) e T18 (UAT) — só o usuário.** Nada commitado (padrão do projeto: nada comita antes do UAT).
- **Última atividade (anterior):** 2026-08-14 — feature `unified-session-across-providers` **ESPECIFICADA** (research + spec + context + design + tasks em `.specs/features/unified-session-across-providers/`). Sucessora de `cross-provider-session-switch`: aquela feature entrega o contexto mas parte a conversa em duas na sidebar, porque o seed cross-provider cria `session_id` nova. Correção = pernas (AD-021). Escopo Large, **21 tasks** (T19–T21 acrescentadas na revisão de contexto do mesmo dia), nenhuma executada, nada tocado no código. Achado da pesquisa que virou requisito: o watcher mapeia artefato em disco → sessão por `getSessionByProviderSessionId` (`sessions.db.ts:355`); sem fallback em `session_legs`, o rollout de uma perna inativa é reindexado como sessão nova e recria exatamente o fork que a feature elimina (T3). Débito assumido: o merge de histórico lê todas as pernas inteiras por página — só otimizar se doer. **Como retomar:** ativar `tlc-spec-driven` e começar por T1 (T4 e T6 rodam soltas em paralelo).
- **Última atividade (anterior):** 2026-08-13 — feature `cross-provider-session-switch` **EXECUTADA por completo (T1–T11 + T14)**, só faltam smoke e UAT. Orquestração AD-012: orquestrador não implementou, 9 workers (Sonnet nas mecânicas, Opus nas críticas). Backend: coluna `seed_primer_path`, `handoff-primer.ts` (histórico normalizado → markdown, 24k chars, cauda), `handoff-seed.ts` (sessão nova no provider destino herdando project/worktree, `loadHistory` injetado por import dinâmico do barril de providers), primer prefixado ao 1º turno em `chat-websocket.service.ts`, fila do run loop ligada + broadcast `session.handoff`. Frontend: hook `useProviderSwitch` (união discriminada `local|confirmation-required|transplanted|queued|seeded|error`), modal do header lista todos os providers, seção Provider/Conta no menu do composer, i18n em 10 locales. **DB-004 e DB-005 fechados.** Task não prevista T14: a premissa "`authenticated` já vem na lista de perfis" era falsa — `toView` não emitia o campo; corrigido na raiz com `isProfileAuthenticated(row)` compartilhado, removendo um N+1 e um fail-open que deixava conta não autenticada parecendo usável. Gate verificado pelo orquestrador: **504/504 testes, typecheck 0, `npx eslint src/ server/` exit 0, build EXIT 0**. NADA commitado (59 arquivos no working tree). **Pendente: T12 smoke com contas reais + T13 UAT** — segue o padrão das features anteriores, nada commita antes do UAT. Armadilha de ambiente: `npm run lint` sai 1 por causa do wrapper npm, não do eslint.
- **Última atividade (anterior):** 2026-08-13 — feature `cross-provider-session-switch`, **Faixa A (backend do contexto) EXECUTADA**. Orquestração AD-012: orquestrador não implementou, 5 workers (Sonnet T1/T2 mecânicas, Opus T3/T4/T5 críticas). Coluna `sessions.seed_primer_path`; `handoff-primer.ts` (markdown do histórico normalizado, 24k chars, cauda); handoff aceita alvo de outro provider criando sessão semeada que herda project/worktree (`handoff-seed.ts`), com `loadHistory` injetado por import dinâmico do barril de providers; primer prefixado ao 1º turno em `chat-websocket.service.ts` e limpo em seguida. DB-004 fechado. Gate: **493/493 testes, typecheck 0, lint exit 0**. NADA commitado. **Pendente:** T6–T13 (fila de turno, notificação ws, frontend, i18n, smoke, UAT). Desvios de implementação registrados em `.specs/features/cross-provider-session-switch/tasks.md`.
- **Última atividade (anterior):** 2026-08-13 — feature `cross-provider-session-switch` ESPECIFICADA (spec + context + design + tasks em `.specs/features/cross-provider-session-switch/`). Escopo Large: trocar claude↔codex (e os 4 providers) no meio da sessão, levando o contexto. Bloqueio hoje é de 4 camadas (estado do chat, menu do composer, modal de conta, `HANDOFF_PROVIDER_MISMATCH` no backend). Achados que viraram trabalho: DB-004 (seed sem consumidor) e DB-005 (fila do handoff sem caller). 13 tasks, nenhuma executada. **Como retomar:** ativar `tlc-spec-driven` e começar por T1/T3 (paralelas).
- **Última atividade (anterior):** 2026-08-09 — feature `multi-account-collab` (feature 2 do AD-011) IMPLEMENTADA. Dois ou mais perfis claude debatem um tópico em rodadas até convergir; árbitro escreve o veredito. Módulo `server/modules/collab/` (repositório, motor com compare-and-set, prompts puros por modo, rotas, composition root), tabelas `collaborations`/`collaboration_turns`, `/api/collaborations` montado em `server/index.js:235`, sweep de órfãs no boot (`:1617`), UI nova aba MainContent (`src/components/collab/`) com polling. Orquestração AD-012: 7 workers, orquestrador não implementou. Gate: **418/418 testes, typecheck 0, lint 0 erros, build EXIT 0**. Revisão achou 2 defeitos bloqueantes invisíveis aos fakes (ordem de registro do resolver de permissão no `claude-sdk.js`; stop sobrescrito pelo veredito) — corrigidos, detalhes em `.specs/features/multi-account-collab/tasks.md`. **PENDENTE: C7, smoke com contas reais** — o revisor avisou que o defeito crítico só aparecia no primeiro turno real. NADA commitado.
- **Última atividade (anterior):** 2026-08-08 — feature `profile-usage` EXECUTADA (spec+design+tasks em `.specs/features/profile-usage/`). Mostra utilização dos limites do plano por perfil em Settings > Profiles. Backend: `server/modules/profiles/usage/` (fetcher claude via `GET api.anthropic.com/api/oauth/usage` com Bearer do `.credentials.json` + headers `anthropic-beta`/`User-Agent claude-code/*`; fetcher codex via `rate_limits` do último rollout JSONL; cursor/opencode `supported:false`), rota `GET /api/profiles/:id/usage`, cache 60s. Frontend: `ProfileUsageMeter` no card do perfil (barras 5h/7d + reset + plano). Gate: **355/355 testes, typecheck 0, lint 0 erros, build EXIT 0**. **PREMISSA registrada no spec.md** (confiança ~85%): "uso" = limites do plano (estilo `claude /usage`), não agregado de tokens — confirmar com usuário no UAT. Token OAuth precisa do escopo `user:profile` (login normal tem; `setup-token` só-inference cai em "unauthenticated"). NADA commitado (segue padrão: worktrees/composer-model-menu também aguardam UAT). **UAT 2026-08-08**: (1) "Could not load usage" = processo systemd antigo, resolvido com restart; (2) perfis ociosos (gdc/jet) mostravam "sign in" com badge Authenticated — accessToken expira em horas e o design original não renovava; T7 adicionou refresh OAuth (`usage/claude-token-refresh.ts`, decisão AD-revertida por evidência empírica: era decisão do Claude, não do usuário). Gate após T7: **357/357**.
- **Última atividade (anterior):** 2026-07-31 — features `worktrees` e `composer-model-menu` EXECUTADAS (port cirúrgico do upstream v1.37.0, AD-016). Backend: módulo `server/modules/worktrees/` portado literal (18 arquivos, +36 testes). Frontend: aba Worktrees no painel Git + `ComposerModelMenu` (modelo + effort) substituindo o chip de Effort. Gate: **292/292 testes, typecheck 0, build EXIT 0, lint 0 erros**. Smoke com git real validou ciclo completo (create→list→merge→remove) e rollback de conflito sem perda de dados. **PENDENTE: UAT do usuário** (T20) e decisão sobre commit — nada foi commitado, 27 arquivos modificados + 12 novos no working tree. Boards: `.specs/features/worktrees/tasks.md`, `.specs/features/composer-model-menu/spec.md`.
- **Última atividade (anterior):** 2026-07-30 — feature `native-install` EXECUTADA no M1 (orquestração AD-012: Fable coordenou, workers Opus N1/N3/N5 + Sonnet N2/N4/N6). Serviço nativo rodando via LaunchAgent em `http://m1:7789` (`installMode: git`); migração validada (perfil Pessoal autenticado, 169 skills, 0 paths `/projects` restantes); KeepAlive testado com kill -9 (ressuscitou <8s). Incidente resolvido: primeiro boot travou em `open()` — TCC do volume externo; usuário concedeu Full Disk Access ao node. Container derrubado. PENDENTES: N6 (remoção deploy/, worker rodando), N8 UAT reboot (usuário), push (aguarda confirmação). Board: `.specs/features/native-install/tasks.md`.
- **Última atividade (anterior):** 2026-07-24 -03 — Rebrand de identidade visível (fora do plano T1–T21, tarefa avulsa a pedido do usuário): UI ainda exibia a marca upstream "CloudCLI" (header da sidebar, telas de auth, Settings > About, título de aba, PWA manifest, fallback de push do service worker, página de API docs) e o badge de estrelas do GitHub apontava pro `siteboon/claudecodeui`. Renomeado para "RFC Code" em todo o app shell + nos 9 locales traduzidos + en (só strings de nome de produto, não texto técnico); badge de estrelas repontado pra `richardfcampos/rfc-code`; atribuição AGPL-3.0 "Based on CloudCLI UI" (AD-008) preservada no footer de auth + nota discreta equivalente adicionada em Settings > About. Fora de escopo mantido intocado: pacote npm, binário/paths `cloudcli`, nomes de classe/módulo, CHANGELOG, outros READMEs (AD-009), CTAs do produto comercial real "CloudCLI Pro/Hosted", listagem do plugin "PRISM CloudCLI", "Maintained by the CloudCLI team" e o version-check contra `siteboon/claudecodeui` (funcional, checa releases reais do upstream). Commit único `feat(branding): rename visible app identity to rfc code` — `34a099d`. Build EXIT 0, lint 0 erros, suíte intacta 209/209 (sem mudança na contagem). Nota detalhada em `.specs/features/mvp-web-harness/tasks.md` § Post-MVP Deviations & Revisions.
- **Última atividade (anterior):** 2026-07-23 21:40 -03 — Revisão de deploy T15/T16 (pós Fase 5): `intel` confirmado como host **macOS** (não Linux) — `network_mode: host` não funciona no Docker Desktop macOS (VM isolada), decisão original inviável em produção. Substituído por bridge networking + `ports:` publicando apenas na interface tailnet (`100.70.101.109:7789`), com contrato `AUTH_TRUSTED_CONTAINER_BIND=1` no guard de bind (`server/middleware/auth.js`) preservando 100% o comportamento default (recusa bind não-loopback sem a env). Commits: `feat(auth): container-aware bind contract for trusted mode` — `6020260`; `fix(deploy): bridge networking with tailnet publish for macos hosts` — `dffc89f`. **209 testes/0 falhas** (base 192 → 200 → 207 → 209); build EXIT 0. Validado de verdade nesta própria máquina (`intel`): smoke completo 7 passed/0 failed, curl do host→container funcionando via publish tailnet (antes só via `docker exec`); container parado ao final (`docker compose down`), nada deixado rodando. Fase 5 (T18–T20) segue concluída e pushed conforme heartbeat anterior. Pendentes de usuário: T17 (deploy intel — agora desbloqueado, imagem/compose validados nesta revisão) e T21 (UAT tablet); depois Verifier.
- **DEPLOY OFICIAL (T17 parte automática) ✅ 2026-07-23 19:09 -03:** `docker compose up -d` executado pelo orquestrador NO intel. Container `rfc-code` healthy; `http://intel:7789` (publish `100.70.101.109:7789→3001`); health/UI 200 pela tailnet; loopback/LAN inacessíveis ✓; volumes: projetos `/Volumes/External Code/INTEL/Code`, dados `~/.rfc-code/data`; NOTIFY_URL/TOKEN injetados (webhook T18 ativo).
- **Fase:** EXECUTE — 21/21 tasks de código concluídas e pushed (Fases 1–5 + fixes; suíte 209/0; HEAD `94e9863`).
- **Pendente (usuário):** T17 parte manual — criar perfis + autenticar contas via terminal web em `http://intel:7789` + sessão real de teste + `docker compose restart` durante sessão (AC3); T21 — UAT tablet. Depois: **Verifier (Opus) obrigatório**.
- **Como retomar:** ativar skill `tlc-spec-driven`; se T17/T21 concluídas pelo usuário, despachar o Verifier (validate.md, autor ≠ verificador) e fechar com validation.md.
- **Clone de verificação:** scratchpad da sessão (`.../scratchpad/claudecodeui`) — shallow, descartável; fork real será clonado no Execute.
- **Artefatos:** `.specs/features/mvp-web-harness/spec.md`, `context.md`.
- **Pesquisas (resumo):** claudecodeui suporta os 4 agents; multi-conta não existe em nenhuma ferramenta (nosso diferencial); Claude Code tem o melhor suporte a perfis headless (`CLAUDE_CONFIG_DIR` + `claude setup-token`); Cursor headless experimental; Vibe Kanban sunsetting, omnara/claude-code-webui arquivados.
